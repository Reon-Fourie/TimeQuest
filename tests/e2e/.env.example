# Base URL of the running dev environment
BASE_URL=http://localhost:5000

# Dev-only auth bypass endpoint is /auth/dev-login?role=<roleName>
# This endpoint must only exist in Development environment.
# Role names: TeamMember, TeamLead, FinancialAdmin, Administrator, SystemAdmin

# Seed user display names (for assertion copy) — these match backend SeedData
TEAM_MEMBER_NAME=Alice Johnson
TEAM_LEAD_NAME=Bob Smith
FINANCIAL_ADMIN_NAME=Deon van der Merwe
ADMINISTRATOR_NAME=Carol Nkosi
SYSTEM_ADMIN_NAME=Eve Pillay
